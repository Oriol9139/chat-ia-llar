# Chat IA Llar

Este es un backend básico en Node.js que permite recibir preguntas sobre tareas por planta y turno, y devuelve respuestas usando la API de OpenAI.

## Instrucciones

1. Renombra `.env.example` a `.env` y pon tu clave de OpenAI.
2. Instala dependencias con `npm install`.
3. Ejecuta el servidor con `npm start`.
